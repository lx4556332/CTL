@model CTL.WEB.Areas.Admin.Models.DegreeViewModel
@{
    ViewBag.Title = "Ступінь";
}

<h2>@ViewBag.Title</h2>
<h4>Видалити ступінь</h4>

<hr />
@Html.ActionLink("Повернутися", "Index", new { area = "Admin", controller = "InputDataDegree" }, new { @class = "btn btn-default", role = "button" })
<hr />

<h4>Ви впевнені, що хочете видалати ступінь @Model.FullName?</h4>
<br />

@using (Html.BeginForm("DeleteDegree", "InputDatadegree", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    @Html.ValidationSummary("", new { @class = "text-danger" })

    @Html.Hidden("Id", Model.Id)

    <div class="form-group">
        @Html.LabelFor(m => m.Name, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Name, new { @class = "form-control", @readonly = "readonly" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-success" value="Видалити" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}